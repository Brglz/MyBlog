{{>header}}
{{#with post}}
<section class="details">
    <div class="detail">
        <h1>Title: {{title}}</h1>
        <h2>Category: {{postCategory}}</h2>
        <h3>Content: {{content}}</h3>
        <a href="#/home">Back to Home</a>
    </div>
</section>
{{/with}}